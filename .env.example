# Server process settings. Paths must be absolute when set.
MIM_DATA_ROOT=/srv/moneyinmotion
MIM_USERNAME=shitals
MIM_PORT=3001

# Optional comma-separated cross-origin allowlist. The production default is
# same-origin only, so this is normally unnecessary.
# CORS_ALLOWED_ORIGINS=https://money.example.com
